import type { LucideIcon } from 'lucide-react';

export interface RoomType {
  value: string;
  name: string;
}

export interface InteriorStyles {
  value: string;
  name: string;
  desc: string;
  icon: LucideIcon;
}
